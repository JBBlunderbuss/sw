const React = require('react');
const ReactDOM = require('react-dom');

/* Import Components */
const People = require('./components/People');

ReactDOM.render(<People/>, document.getElementById('main'));